# Dependências
node_modules/

# Segredos — NUNCA versionar (.env.example é a exceção, sem valores reais)
.env
.env.*
!.env.example

# Builds
dist/
build/

# Logs
*.log
logs/

# Uploads locais (faturas/áudio em desenvolvimento; em produção vivem em /var/lib/bigbag)
uploads/

# Faturas reais (dados pessoais — NIF, compras). Nunca versionar.
imagens/
*.jpeg
*.jpg

# Sistema operativo / editores
.DS_Store
Thumbs.db
.vscode/
.idea/

# Perfis nutricionais / dados clínicos (sensíveis — nunca versionar)
*Perfil*.txt
*perfil*.txt
docs/*Perfil*

# Token rotativo do Lidl Plus (segredo, nunca versionar) — só o FICHEIRO do token,
# NÃO o módulo lidlplus_token.js. Padrão específico ao ficheiro com ponto inicial.
.lidlplus_token
*/.lidlplus_token

# experimentos locais de imagem (nao versionar)
tmp_img/

# artefactos de inferencia (modelos, cache)
backend/infer/cache/

tmp_ui/

# artefacto do pacote para o designer (regeravel)
BigBag_Pacote_Designer.zip
tmp_v2design/
