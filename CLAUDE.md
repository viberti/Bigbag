# CLAUDE.md — Regras de trabalho neste projeto

Projeto pessoal de laboratório, **utilizador único**. PWA (React/Vite) + backend Node/Express + MySQL, no servidor de produção partilhado `85.25.46.6` (mesmo host de `pitacos.ai` e `1417`). App de histórico de preços de compras: lê faturas, guarda os dados, responde a consultas por nota de voz.

## Princípio geral
Trabalha com autonomia máxima. Decide sozinho tudo o que for **local e reversível** — estrutura de código, refactors, queries, migrações na BD do próprio projeto, escolha de bibliotecas, commits. Não peças permissão para isso. Pergunta o mínimo: quando algo for ambíguo e **reversível**, escolhe a opção razoável, segue em frente, e regista a suposição.

## Paragens obrigatórias (a ÚNICA lista de coisas a confirmar antes de executar)
Confirma comigo, mostrando o que vais correr, ANTES de:
1. Qualquer comando que toque em **serviços partilhados** do host — MySQL global, Apache, systemd, UFW, certbot — de forma que possa afetar `pitacos.ai` ou `1417`.
2. Qualquer **SQL de DDL/migração destrutiva** (DROP, TRUNCATE, ALTER que perca dados). Mostra o SQL antes. `GRANT` só em `app_<PROJ>.*`, nunca noutras bases.
3. **Apagar ou sobrescrever ficheiros** fora da árvore do projeto, ou ficheiros de dados/uploads já existentes.
4. **Reescrever histórico partilhado** (force-push, rebase de ramos partilhados). *(`git push` normal — incluindo para `main` — já NÃO precisa de confirmação.)*
5. **Revogar o sudo temporário** (`/etc/sudoers.d/90-<PROJ>-nopasswd`) — fá-lo no fim da instalação e confirma que ficou feito.
Fora desta lista, age sem perguntar.

## Isolamento (regra dura — herdada do runbook)
- Nunca tocar em MySQL/Apache/serviços dos outros projetos sem avisar.
- BD `app_bigbag`, user MySQL `bigbag` (GRANT só em `app_bigbag.*`), porta local **`4200`** (confirmada livre). Serviço `bigbag-backend.service`.
- Projeto corre sob o **user Linux partilhado `dev`** em `/home/dev/bigbag` (decisão do dono — reusa o `dev`, que já aloja o 1417; não há user dedicado). Domínio `bigbag.hal9klabs.com`.
- `.env` com `chmod 600`, **nunca versionado**. `.env` no `.gitignore` desde o primeiro commit. Segredos só em variáveis de ambiente, nunca hardcoded no código nem em ficheiros que vão a commit.
- Setup de servidor reutilizável (com segredos) vive **fora do repo**, no PC: `C:\ProjetosAI\Setup_Ambiente_Novo_Projeto_PRIVADO.md` (nunca comitar).

## Testar antes de dar por feito (obrigatório)
Uma tarefa só está concluída depois de verificada a funcionar — não basta compilar:
- Backend: o endpoint responde como esperado (curl ao `/health` e à rota nova); a migração aplica sem erro.
- As funções de tool use devem ser testáveis **por texto** antes de haver voz.
- Frontend: a build passa (`npm run build`) e o fluxo principal funciona no browser.
- Lógica pura (reconcile, formato) tem testes em `backend/test/*.test.mjs`. Os testes que tocam BD/LLM só correm no servidor (não há MySQL local no PC) — corre os de lógica com `node --test test/<ficheiro>.test.mjs`.
- Se algo não dá para testar automaticamente, di-lo explicitamente e descreve como verificaste à mão.
- Não marques nada como "feito" com um teste a falhar ou por correr.

## Git
Decide o fluxo conforme o caso (commits pequenos e focados quando ajuda; agrupar quando faz sentido). Mensagens de commit claras e em português. **`git push` é livre — não precisa de confirmação** (decisão do dono, 2026-06-06). Só **reescrever histórico partilhado** (force-push, rebase de ramos partilhados) é que continua a ser paragem obrigatória.

## Documentação — fontes de verdade
Mantém estes documentos atualizados **após cada alteração que mude o que neles está descrito** (não a cada commit trivial):
- **`Conceito_Historico_Precos_Voz.md`** — conceito, arquitetura, decisões. Atualizar quando uma decisão em aberto fechar ou a arquitetura mudar.
- **`Schema_e_Funcoes_ToolUse.md`** — schema da BD e contrato das funções. Atualizar sempre que o schema ou uma função mude.
- **`Normalizacao.md`** — normalização **atual** (problema, dificuldades, solução, lacunas). Atualizar quando a normalização mudar.
- **`Taxonomia_Produto.md`** — modelo-alvo **facetado** (standards OFF/GS1/IFPS, níveis, coorte, Produto Mestre). Atualizar quando uma decisão de desenho fechar; é o norte da migração incremental.
- **`Visao_Conselheiro_Saude_Alimentar.md`** — **visão** para além do preço: o Bigbag como conselheiro de saúde alimentar do agregado (nutrição herdada da classe facetada via OFF, EAN opcional/incentivado, estimativa com confiança, princípio "factual não clínico", reuso de LanguaL/FoodOn/OFF). Atualizar quando a direção evoluir.
- **`Paper_Resolucao_Produtos_Talao.md`** — relatório técnico (paper) do método de resolução de entidades de produtos a partir de talões.
- **Runbook de bootstrap** (versão limpa, sem segredos) — passos de servidor. Atualizar se o processo de deploy/infra mudar.
- Quando fechares uma "decisão em aberto" (transcrição, leitura de fatura, autenticação), regista a escolha e o porquê no documento de conceito.

## Estado atual (atualizar periodicamente — 2026-06-09 · app v0.83.0)
- **Catálogo + matching de EAN (Fase 2 — 2026-06-09):** para dar EAN/ficha aos itens do talão **sem EAN**, construiu-se um **catálogo por nome→EAN** (`catalogo_produto`, `backend/scripts/scrape_catalogo.mjs`; só **Auchan/Continente** expõem EAN em HTML estático — sitemaps + JSON-LD `gtin13` / `&ean=`; o resto (Lidl/PingoDoce/ECI/...) é app/API-gated). **Resolvedor** (`src/normaliza/resolverProduto.js`): junta candidatos do catálogo + Open Food Facts, pontua por tokens/marca/formato, **despriorizia EANs internos de loja** (prefixo 2 = peso variável, sem GTIN/nutrição). **Mestre por EAN** (`src/normaliza/mestreEan.js`, distinto de `mestre.js`/taxonomia): agrega variantes de nome + nutrição de **todas** as fontes — a tese (EAN como chave canónica enriquece o match; cobertura forte subiu 67%→70%). **Aba EANs no `/admin`**: *gerar propostas* (candidatos do catálogo, **sem LLM — o operador é o juiz**) → *aprovar/corrigir/rejeitar*; ao aprovar, o produto ganha **ficha** (nutrição via OFF pelo `mestrePorEan`) e o nome do talão vira **variante conhecida** do EAN (ciclo virtuoso). **Frescos excluídos** do matching (nutrição vem do nome via `produto_generico`). Scripts: `match_cobertura.mjs` (cobertura, rápido), `match_dryrun.mjs` (com LLM, valida vs EANs conhecidos). **Lidl Plus API: BECO SEM SAÍDA** — o token OAuth obtém-se (`scripts/lidlplus_auth.mjs`, PKCE S256), mas a API de talões é bloqueada por **Akamai Bot Manager** (TLS fingerprint + sensor; imbatível sem o app real, provado com curl_cffi); ficheiros guardados p/ futuras fontes de talão digital. **Pendente:** terminar scrape do Continente (gentil; parou ~19k/98k por rate-limit) + refinar discriminação de variantes do juiz.
- **Ícone novo "Spotlight" (v0.82):** saco-mascote (carinha + spark) iluminado por holofote verde sobre fundo escuro — substitui o saco branco sobre verde chapado (confundia-se com o WhatsApp). Assets web em `frontend/public/app-icons/` (PNGs 48–512 + maskable 192/512 compostos do adaptativo) + `public/icon.svg` master; manifesto com `any`+`maskable`, `theme_color #0c3b32`/`background_color #06201d`, apple-touch-icon 180. **Handoff completo (incl. ícone adaptativo Android: foreground/background SVG+432) em `design/appicon/`** — pronto para a build nativa (Capacitor) quando avançar. A logo dentro da app (`<Mark>`) NÃO foi mexida (é o cabeçalho, handoff era só o ícone da app).
- **Infra:** FECHADA. `https://bigbag.hal9klabs.com` público (Apache vhost + Let's Encrypt + redirect), `bigbag-backend.service` (systemd, enabled, auto-restart, porta 4200). BD `app_bigbag`, migrações aplicadas até **030** (013–018 como antes; **019** `categoria_nutricao`; **020** `produto_ean` — identificação de produto por EAN (id, ean UNIQUE, sku_id, item_id, nome/marca/.../nutricao, vlm_json, off_json; item_id NULL = produto consultado, sem nota); **021** `produto_foto` — fotos do rótulo por item (em `/var/lib/bigbag/produtos/`); **022** `produto_analise` — cache da análise factual (chave EAN ou `sku:<id>`); **023** `produto_generico` — classificação por SKU fresco/processado + nutrição típica dos frescos; **024** `produto_nome` — todas as variantes de nome por produto (talão/canónico/vlm/off); **025** `nome_sugestao` — sugestões de nome canónico p/ o operador; **026** `perfil_membro` — perfil nutricional por membro; **027** `item.ean` — EAN-13 da linha do talão (cash-and-carry/Makro), validado por dígito verificador, **autoritativo** sobre o lido à mão; **028/029** `catalogo_produto` — catálogo multi-fonte com EAN por nome PT (`fonte`+`sku_fonte` UNIQUE, nome/marca/categoria_path/preco/url/imagem); **030** `match_ean_sugestao` — propostas de EAN por matching de nome p/ o operador rever na aba EANs (uma por descrição; estado pendente/aprovado/rejeitado)).
- **Eixo NOVO — conselheiro de saúde alimentar / assistente nutricional (2026-06-08):** o Bigbag deixou de ser só preço. **Identificação de produto** por EAN (scan de barras com `@zxing/browser`, foto do EAN, ou EAN da linha do talão) + fotos do rótulo → VLM lê o rótulo e Open Food Facts pelo EAN. **Ficha factual** (não clínica): selo Nutri-Score, NOVA, semáforo UK FSA, avisos "ALTO EM" (estilo Chile), tabela nutricional, ingredientes com E-números, **parecer** estilo nutricionista. **Frescos** (fruta/legume/carne) sem EAN ganham nutrição típica pelo **nome** (LLM ≈ tabela oficial) → ficha sem foto. **Perfil nutricional por membro** (carregar ficheiro gerado por outro LLM ou colar) → avaliação personalizada do produto: alertas determinísticos de alergia/intolerância (sinónimos PT↔EN/OFF) + veredicto/parecer LLM. Princípio: **factual, não clínico** — aplica as regras do perfil (de nutricionista/pessoa), não diagnostica; o ficheiro do perfil é **dados, não instruções**; dados clínicos **não versionados** (`*Perfil*.txt` no `.gitignore`). **Modelo de 3 níveis de nome:** nota (`descricao_original`, abreviatura) → produto real (por EAN, com marca, ao nível do item) → nome normalizado (`sku.nome_canonico` = família genérica, **sem marca**). Detalhe em `docs/` (Schema, Conceito, Arquitetura, Normalizacao, Taxonomia, Visao_Conselheiro, Paper — todos atualizados 2026-06-08).
- **Três superfícies** (routing por path em `frontend/src/main.jsx`):
  - **App de chat (`/`)** — PWA do utilizador: envia notas (📷 câmara **inteligente**: se a foto for um código de barras → consulta o produto; senão → talão), faz perguntas, e tem **carrinho** (habituais → 🛒). Rodapé: câmara · menu (⋯) · notas · **scanner ciano** (barras). Menu (⋯): digitalizar/galeria/ficheiro · despensa · gastos · por identificar · perfil nutricional. **Despensa** (produtos com EAN, por compra), **Gastos** (mês atual/anterior/média/série/por loja, drill-down às compras), **scanner de código de barras** (`@zxing/browser` ao vivo + foto + manual) + **consultar produto por EAN** (base→OFF, guarda p/ futuro), **perfil nutricional** por membro. "As minhas compras" **redesenhado**: cartões por loja (cor+monograma) agrupados por mês + detalhe deslizante. Abrir um produto → **ficha factual** (Nutri-Score/NOVA/semáforo/aditivos/parecer) + **avaliação personalizada** se houver perfil ativo.
  - **Operador (`/admin`)** — desktop: gerir SKUs canónicos (renomear, associar/dissociar, fundir, auto-merge), rever notas, editar quantidade, qualidade por cadeia/origem, `nome_simplificado`, **aba Revisão** (worklist por confiança), **aba Ligar nomes**, **aba Nomes** (sugestões de nome canónico **genérico** geradas por LLM das variantes em `produto_nome` → aplicar/rejeitar, com guarda anti-colisão), e **aba EANs** (matching de nome do talão → EAN do catálogo: gerar propostas → aprovar/corrigir/rejeitar → o produto ganha ficha+nutrição).
  - **Comprador (`/explorar`)** — desktop, tema "talão": explorar produtos, preço pago vs por-unidade, variação (gráfico), por mercado, com seletor de mês/ano.
- **Ingestão (Bloco 2):** a funcionar. `POST /api/faturas` (auth): VLM direto p/ imagem, OCR-texto+LLM p/ PDF → extração → reconciliação (sinal honesto `discrepancia`; **desconto de cartão NÃO é espalhado** pelos itens — `preco_liquido` = preço impresso) → normalização (formato → `preco_por_base`) → canonicalização inline (LLM) + auto-merge de nomes idênticos. `origem_captura` regista o caminho de captura, para comparar leituras.
- **Consulta (Bloco 3):** **11 funções** + tool use, testadas (texto), expostas em `POST /api/consulta` (texto) e `POST /api/voz`. Funções: `buscar_ultima_compra`, `comparar_precos_por_loja`, `produtos_habituais`, `detalhes_fatura`, `produto_mais_barato`, `historico_preco`, `listar_compras`, `total_gasto`, `tendencia_precos` (produtos que subiram/desceram), `comparar_lojas` (cadeia mais barata p/ o usuário), `lembrar`. **Modelo de consulta = `gemini-2.5-flash` (full)** — o flash-lite era inconsistente em tool-use (ora chamava, ora devolvia vazio); a consulta é fração mínima do custo. Defensivas: normaliza prefixo do nome da tool (`default_api.*`) + guarda anti-resposta-vazia.
- **Auth:** OAuth configurado no `.env`, a aguardar redirect URI na Google Console. Entretanto, **portão temporário** `ENABLE_TEST_AUTH` (HTTP Basic, users `gustavo`/`sue`) protege as rotas. Trocar pelo OAuth quando pronto.
- **Sudo temporário** `90-bigbag-nopasswd` ainda ativo (instalação não terminou).
- **Extração estruturada de peso (2026-06-07):** o esquema do VLM passou a ter **`peso_kg` + `preco_base_impresso`** (€/kg) em campos PRÓPRIOS; `descricao_original` = só o nome. `normalizarItens` reconstrói o `linha_peso` canónico desses campos (autoritativo, sem regex) → o €/kg vem direto do talão na origem (recupera a causa 2 da lacuna). Validado por re-extração real (Lidl #35: BANANA peso_kg=1.134/€1.19, ALPERCE 0.362/€4.99…). Regex no `normalizarItens` fica como rede de segurança (PDF-texto/retrocompat).
- **Limpeza de descrições (2026-06-07):** o nome capturado já NÃO carrega qtd/peso/preço/IVA. `normalize.normalizarItens` corre `limparDescricao` como passo final (preserva o peso em `linha_peso` antes de o tirar — o `reprocess` calcula ppb de `descricao_original`+`linha_peso`). `matcher.resolverSku` passou a usar a **descrição limpa como chave de alias** (antes a chave crua tinha o peso variável → a mesma banana nunca reusava o alias). Backfill aplicado (`scripts/limpar_descricoes.mjs`, idempotente): 157 descrições de `item` limpas, 148 `sku_alias` fundidos (632→488), mamão (#2300) dissociado da Banana. Ex.: Banana 8 descrições→4, com as 14 compras consolidadas em "BANANA".
- **Deduplicação robusta (2026-06-07):** a dedup antiga (scoped a `loja_id`, por nº-doc OU data+total) falhava quando o VLM lia mal o **nome da loja** (Mercadona→"Irmadona" → loja_id diferente) ou a **data**. Adicionada rede 2 em `persist.js`: **cadeia + total + nº de itens**, confirmada por **sobreposição de preços** (`sobreposicaoPrecos`, tolera ±0,02 de OCR). Corrigido o estrago: loja "Irmadona" #17 fundida no Mercadona; **3 faturas duplicadas apagadas** (#197/#192/#239); €/kg recuperável re-extraído (só 1 item — o resto é causa 1).
- **Reforço extração/dedup/EAN (2026-06-08):** (a) **dedup endurecida** (`persist.js`) — nº do documento por **cadeia** (sobrevive a nome de loja mal lido), Rede 1 por `DATE()` (ignora a hora), **assinatura forte** `cadeia+DATE+total+nº itens` (sem precisar de sobreposição de preços) + a sobreposição como rede final; removidos **4 pares de duplicados** que tinham escapado (ALDI, Continente, 2× Pingo Doce). (b) Captura do `numero_fatura` reforçada (regra ATCUD/FS/Nº). (c) **`eanValido`** (dígito verificador GTIN, `test/ean.test.mjs`) usado em toda a identificação/consulta — rejeita EANs mal lidos. (d) **EAN da linha do talão (Makro)**: capturado em `item.ean`, validado, autoritativo; enriquecido pelo OFF na ingestão. **Caveat:** o VLM pode ler um EAN errado mas válido (papel higiénico 540→560 = fórmula NAN) → backlog: cruzar com a descrição/busca web (Brave) p/ não-alimentares. (e) **marca limpa** (tira holding 'SONAE' e a lista do OFF).
- **Itens a peso sem peso na nota (2026-06-07):** migração **018** `item.peso_em_falta`. Quando o produto é a peso (unidade kg/L) mas a nota não traz peso, `ppb=NULL` (incomputável honesto, já era) + `peso_em_falta=1` (marca o porquê) → fora do €/kg sem fingir um €/peça enganador. `decidirUnidadeBase`: "partido/partida/metade/granel" → kg (peça cortada a peso, ex.: mamão partido). Mamão (#609) corrigido un→kg. **193/593 itens** marcados. Caso validado pela própria imagem da nota #198 (mamão sem linha de peso; banana/batata com).
- **⚠ Lacuna de €/kg (investigada 2026-06-07):** **216/626 itens (34,5%) sem `preco_por_base`** — kg 60%, L 53%, un só 4%. **Re-extração testada (causa 2): só 1 item recuperável** — o resto é **causa 1** (a loja não imprime €/kg: Mercadona/Aldi dão só o preço da peça). Resolvido honestamente com `peso_em_falta` (acima). **Derivação por pacote fixo (2026-06-07):** quando o produto é a peso mas a linha não traz peso E o SKU é um **pacote fixo fiável** (`formato_valor` real ≠1, nunca pesado ao balcão, sem marcador KG/GRANEL nem multipack na descrição), o `ppb` deriva-se de `preço / tamanho do pacote` (`ppb.js` `pacoteFixoFiavel`). Recupera ex.: **mirtilo 500g→10,30€/kg**, salada, café descafeinado, passata, kefir, lixívia (8 itens). NÃO deriva p/ itens pesados (banana/carne) nem `formato_valor=1` default → evita €/kg fabricado.
- **Backlog / pendências menores:** re-enviar #202/#203/#205 (preços impressos limpos); operador corrigir 3 ovos truncados do Lidl no `/admin`; lote da manhã (#86–93) com preços ligeiramente raspados (batem na mesma).
- **🔎 Busca por EAN na web p/ NÃO-alimentares (a fazer mais tarde — 2026-06-08):** o OFF só tem alimentos; papel higiénico/detergentes/etc. não estão lá. O Google **encontra** o produto por EAN nas páginas de retalhistas PT (ex.: vinho do Makro → El Corte Inglés "Q.M. Espumante Bruto Alvarinho 75cl"); o título do resultado já chega (não é preciso abrir a página, muitas bloqueiam bots/403). **Falta:** uma API de busca com região PT — **Brave Search API** (grátis 2000/mês, `country=PT`, recomendada) ou Serper.dev (`gl=pt`) ou Google CSE; chave no `.env` (não versionar). Pipeline: EAN não no OFF → buscar na web → **cruzar com a `descricao_original` do talão** (se o produto encontrado NÃO bate, ex.: "PAP.HIG" → "Leite NAN", o EAN foi mal lido → não atribuir + marcar suspeito) → guardar nome/marca/descrição. O teste com o web-search do OpenRouter (`:online`) foi **fraco** (deu fórmula NAN p/ o papel higiénico e null p/ o vinho) → não usar; precisa de motor a sério + região PT.
- **⚠ EANs lidos do talão (Makro) podem estar ERRADOS mesmo com dígito verificador válido:** caso real — papel higiénico `5401856230167` foi lido `5601856230167` pelo VLM (trocou 4→6), e esse é um EAN válido e real de OUTRO produto (fórmula NAN). O dígito verificador NÃO apanha estes. Mitigação: para food, o OFF auto-confirma (categoria bate); para não-food, cruzar com a descrição do talão (ver ponto acima) ou confiar só no **scan real** das barras. Considerar marcar EANs vindos do talão como "provisório/a confirmar".
- **⚠ Identificação de produto — fotos do produto ERRADO (a tratar):** acontece o utilizador fotografar outro produto que não o item da nota (caso real: item 2934 "KEFIR LIQ…" recebeu fotos/EAN/análise de um **Skyr**). Dados errados removidos à mão a 2026-06-08 (item 2934 limpo → volta à câmara; item 3197 tinha 2 identificações, removida a de EAN inválido `8480000000000`). **Falta implementar** (estava a meio, revertido): (1) **prevenir** — passar a `descricao_original` da nota ao VLM em `extrairProdutoFotos` e pedir veredicto `corresponde_nota`/`corresponde_motivo`; a app avisa se a foto for claramente outro produto; (2) **corrigir** — endpoint `DELETE` + botão "Remover identificação / refazer" no `ProdutoInfoSheet` (apaga `produto_ean`+`produto_foto`+ficheiros+`produto_analise` do item e reverte ao ícone da câmara). **Duplicação em `produto_ean` CORRIGIDA (2026-06-08):** o `INSERT ... ON DUPLICATE KEY UPDATE` só dedupe por `ean` (UNIQUE); com `ean=NULL` (identificação só por foto) cada re-identificação criava nova linha → duplicados na despensa (ex.: Ricotta ×4, carne ×2, + linhas vazias em itens que JÁ tinham EAN, como o Frosties). Guarda nova na rota `/identificar`: re-identificação de um item **apaga as fichas anteriores SEM EAN** antes de inserir (substitui, não acumula); as fichas COM EAN ficam (catálogo por EAN). Limpeza retroativa: 72→63 linhas (9 redundantes; os 6 itens afetados ficaram com 1 ficha). NÃO se usou índice único em `item_id` (partiria o modelo por-EAN).
- **🛒 Lista de compras PREDITIVA (backlog — ideia validada por tese externa, 2026-06-08):** hoje há "habituais"/"carrinho", mas falta a previsão **"para X dias → N unidades"**. Algoritmo (da tese MSc *Shopping List Automator*, Hugo Santos, UFP 2021 — antepassado académico do Bigbag): por produto, juntar todas as compras (`item`: quantidade, data, preço) → datas mais antiga/recente → estimar **período de consumo → consumo médio diário** + preço unitário médio → subtrair stock estimado em casa → devolver lista (produto, quantidade, preço estimado, total). Usa dados que **já temos**. A mesma tese **valida** a escolha do Bigbag (mediu OCR/Tesseract em foto = ~50% itens vs PDF 100% → confirma "VLM > OCR" e "PDF-texto > foto-OCR"). Outras ideias da tese: **união de fotos** p/ talão comprido; **harness de avaliação** (extração vs JSON esperado).
- **👪 Agregado familiar (backlog):** perfil de **consumo partilhado** pela casa (vários membros contribuem faturas → um só perfil de consumo p/ a lista preditiva). Encaixa no eixo `perfil_membro` já existente (perfis nutricionais por membro). Também sugerido como evolução na tese acima.
- **📷 Otimizações de captura no browser (2026-06-08, FEITAS):** (a) **redimensionar a imagem antes do upload** (`api.js` `redimensionarImagem`, canvas + EXIF) — limita pela **LARGURA** (não pelo lado maior; a legibilidade do talão depende da resolução horizontal das colunas) → notas **largura 1100/q0.72** (~450–500 KB na foto inteira c/ fundo; ~285 KB se recortada), identificação 1400/q0.80, EAN 1600/q0.85. Upload ~5× mais leve, leitura intacta. **Limites testados (v0.81):** abaixo de ~900px de largura ou ~400 KB parte a leitura; mas a variável dominante NÃO é o tamanho — é a **variância do VLM** neste tipo de talão (papel térmico + linhas a peso €/kg que ele às vezes parte → 24 itens): a 700 KB também falha ~1/4 em tiro único. O que garante a leitura certa é o **loop de auto-correção** da ingestão (re-pergunta quando não reconcilia), que cobre qualquer destes tamanhos. "Pequeno E limpo" só com recorte (removido) ou scanner nativo (futuro). (b) **miniatura local imediata** da foto na bolha do chat (feedback instantâneo). (c) **deskew/EAN ao vivo** já existiam (zxing na câmara). App em **v0.78**.
- **🧾 Talão "certinho" (recorte/endireitar) — INVESTIGADO a fundo, decisão tomada (2026-06-08):** o objetivo é guardar uma versão limpa do talão para servir o VLM **E** mostrar ao utilizador. **O nosso dewarp não chega:** (1) `digitalizar`/jscanify no browser (pós-captura) faz recorte por caixa que **deixa fundo e inclinação** (testado na #247: ficou torto + tarja preta à esquerda); ligá-lo a todos os caminhos da câmara (v0.79) **degrada** fotos boas. (2) Heurísticas no servidor (só `sharp`): **limiar de brilho falha** — mesa escura **com textura** salpica falsos claros (#247), e **papel sobre madeira clara** não tem contraste nenhum (#198); **PCA do ângulo** é ambíguo em folha quase quadrada (A4 Makro #140 continua torto). Conclusão: segmentação robusta de documento sobre fundo arbitrário é problema de **ML**, não de limiares. **DECISÃO:** o melhor caminho é o **scanner nativo do telefone** (VisionKit/ML Kit — deteção de bordas + endireitar + realce, estado-da-arte). Uma PWA **não consegue invocar** o scanner nativo (não há API web; `<input capture>` só dá modo foto). → **Embrulhar a app em Capacitor** (casca nativa, mesmo código React) e usar um plugin de document scanner. **Começar pelo ANDROID** (APK por sideload, sem loja; plugin ML Kit). **A verificar quando avançar:** toolchain Android no PC (JDK + Android SDK + gradle/Android Studio — **NÃO confirmada**, o dono não quis instalar agora); nome exato do plugin npm (ML Kit document scanner para Capacitor); **auth/CORS** no webview nativo (provável usar `server.url=https://bigbag.hal9klabs.com` para carregar o site real e manter same-origin + a auth Basic do localStorage, com o bridge do Capacitor injetado); build do APK + instalar no telemóvel. iOS fica para depois (assinatura/Xcode). **RESOLVIDO (v0.80, 2026-06-08):** **auto-dewarp REVERTIDO** na câmara/galeria — as fotos voltam a ir diretas (só o redimensionar por largura da v0.78), **sem jscanify automático**. Com o dewarp off, a imagem guardada é o **original** (resolve também o risco de destruir a foto boa). Decisão do dono: esquecer o recorte por agora; quando a PWA virar **app nativa** (Capacitor/Android), o scanner nativo resolve isto com qualidade — é o benefício extra que justifica o salto. **Nota jscanify:** a última versão (1.4.2, jan 2026) **não traz melhorias** — mesma API de 4 métodos, método limitado por desenho (Canny+Otsu+maior contorno) que falha em baixo contraste/fundo texturado; serve só como motor de *warp* se os cantos vierem de fora (VLM/nativo). O **scanner guiado** (`/` → Camera com `digitalizar`/`detectarPapel`) ainda usa jscanify como **opt-in explícito** — não corre automaticamente; remover de vez é opcional. Fluxo nativo que já funciona sem código: digitalizar no app do telefone → guardar como **imagem** → enviar pela galeria.

## Decisões ainda em aberto (não inventar — usar a opção segura e assinalar)
1. **Transcrição de voz:** STT separado vs. áudio-direto ao LLM. **v1 em uso:** áudio-direto via chat (`input_audio` base64), de forma trocável em `transcricao.js`. Falta experimentar STT-separado antes de fixar.
2. ~~**Leitura de fatura:** VLM direto vs. OCR+LLM.~~ **FECHADA (2026-06-07).** Por tipo de ficheiro: **VLM p/ imagem (foto/scan), texto-do-PDF+LLM p/ PDF**; `fatura.metodo_extracao` regista qual gerou cada registo (`ocr_llm` é, na verdade, "texto-do-PDF+LLM" — não há OCR real). Dois head-to-heads: **(a) PDF** (`compara_extracao.mjs`, 16 PDFs) → texto+LLM ≥ VLM (reconciliam 16/16 vs 15/16, |disc| 0,000 vs 0,054); **(b) FOTO** (10 fotos) → VLM-direto ≫ Tesseract→LLM (reconciliam **9/10 vs 3/10**, total certo 10/10 vs 6/10, |disc| 0,011 vs 1,919 — OCR de foto leva o LLM a **alucinar** talão a partir de ruído). **Custo é neutro entre modalidades** (medido via `usage.cost`/`custo_chamada`): só o modelo pesa (flash ~5× lite), não imagem-vs-texto. OCR dedicado p/ fotos **rejeitado**. Ver `docs/Conceito_Historico_Precos_Voz.md` §4.
3. ~~**Autenticação**~~ **FECHADA (2026-06-04):** servidor exposto à internet → Google OAuth + `SUPERUSER_EMAIL`. As rotas exigem sessão (portão temporário até o OAuth ficar ativo).

## Internacionalização (i18n) — base PT-BR, código localizável
- **Idioma base: português do Brasil (PT-BR)**, tratando o usuário por "você". Mas **nunca hardcodar texto visível** ao usuário — codificar de forma a permitir tradução fácil.
- **Frontend:** todo o texto da UI passa por `frontend/src/i18n.js` via `t('chave', vars)` (interpolação `{var}`, plural `{n|sing|plur}`, deteção do idioma do browser). **Traduzir = adicionar um dicionário** de idioma; os componentes não mudam.
- **Backend (respostas do assistente):** o idioma está centralizado no system prompt (`consulta.js`); pode passar a locale-driven quando houver 2.º idioma.
- **Exceções (não são UI, ficam como dados):** prompts internos de extração de nota e os nomes de produtos canónicos (vêm de notas de supermercados de Portugal).

## Notas técnicas
- IA toda via **OpenRouter** (compatível OpenAI), uma só chave (`OPENROUTER_API_KEY`), cobre texto/imagem/áudio. Áudio vai em base64 (URLs não suportados para áudio).
- **Modelo de extração = `gemini-2.5-flash`** (`OPENROUTER_MODEL_EXTRACAO`). Benchmark de 5 VLMs (2026-06-07, `scripts/compara_vlms.mjs`, 20 imagens): o mais barato (`qwen`/`flash-lite`, ~$0,11/100 notas) erra preços/quantidades e perde itens → **falsa economia**; o **custo é não-fator** (<$0,01/nota); a geração 3.x supera a 2.5 (`gemini-3-flash-preview` lidera, mas é *preview*). Mantido o `gemini-2.5-flash` (estável, 20/20 itens). Detalhe no Conceito §4.3.
- `OPENROUTER_TIMEOUT_MS=20000` herdado — vigiar; pode ser curto para imagem de fatura grande num VLM. Subir se necessário.
- Correspondência "produto em linguagem natural" → SKU canónico é trabalho do **backend** (fuzzy match / embeddings), não do prompt do LLM.
- Comparações de preço usam sempre `preco_por_base` (€/kg, €/L, €/un); filtrar `is_clearance` e `is_non_product`.
- PWA: testar câmara e microfone em **dispositivo real** (sobretudo iOS/Safari), não só no desktop.
