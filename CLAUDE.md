# CLAUDE.md — Regras de trabalho neste projeto

Projeto pessoal de laboratório, **utilizador único**. PWA (React/Vite `frontend/`) + backend Node/Express (`backend/`) + MySQL, no servidor de produção partilhado `85.25.46.6` (mesmo host de `pitacos.ai` e `1417`). App de histórico de preços de compras: lê faturas, guarda os dados, responde a consultas por nota de voz e gere uma lista de compras partilhada.

> **Este ficheiro é o PANORAMA + as regras.** O detalhe (arquitetura, schema, decisões, histórico) vive nos docs-fonte (ver §Documentação). Quando algo aqui crescer além de um parágrafo, move o detalhe para o doc e deixa só o ponteiro.

## Princípio geral
- **Autonomia máxima.** Decide sozinho tudo o que for **local e reversível** — estrutura de código, refactors, queries, migrações na BD do projeto, bibliotecas, commits. Não peças permissão. Ambíguo + reversível → escolhe a opção razoável, segue, regista a suposição.
- **Caso específico → regra GERAL** (dono, 2026-06-13): uma correção de classificação/normalização tem de criar lógica aplicável a um conjunto maior de produtos, nunca regras para artigos individuais. *Exceção deliberada:* produtos muito populares podem ter tratamento de **enriquecimento** especial (mais info, fontes próprias) — nunca de classificação.

## Paragens obrigatórias (a ÚNICA lista a confirmar antes de executar)
Mostra o que vais correr e confirma comigo ANTES de:
1. Comando que toque em **serviços partilhados** do host (MySQL global, Apache, systemd, UFW, certbot) de forma que possa afetar `pitacos.ai`/`1417`.
2. **SQL DDL/migração destrutiva** (DROP, TRUNCATE, ALTER que perca dados) — mostra o SQL. `GRANT` só em `app_bigbag.*`.
3. **Apagar/sobrescrever ficheiros** fora da árvore do projeto, ou ficheiros de dados/uploads existentes.
4. **Reescrever histórico partilhado** (force-push, rebase de ramos partilhados). *`git push` normal — incl. `main` — é livre.*
5. **Revogar o sudo temporário** (`/etc/sudoers.d/90-bigbag-nopasswd`).

Fora desta lista, age sem perguntar.

## Isolamento (regra dura)
- Nunca tocar em MySQL/Apache/serviços dos outros projetos sem avisar.
- BD `app_bigbag`, user MySQL `bigbag` (GRANT só em `app_bigbag.*`), porta local **`4200`**. Serviço `bigbag-backend.service`. Domínio `bigbag.hal9klabs.com`.
- Corre sob o **user Linux partilhado `dev`** em `/home/dev/bigbag` (reusa o `dev`, que já aloja o 1417). O `.env` é 600 do `dev`; o ssh entra como `pitacos` → correr scripts como `sudo -u dev node --env-file=.env …`.
- `.env` **nunca versionado** (no `.gitignore` desde o 1.º commit), `chmod 600`. Segredos só em variáveis de ambiente, nunca hardcoded nem em ficheiros que vão a commit.
- Setup reutilizável **com segredos** vive fora do repo, no PC: `C:\ProjetosAI\Setup_Ambiente_Novo_Projeto_PRIVADO.md` (nunca comitar).

## Testar antes de dar por feito (obrigatório)
Uma tarefa só está concluída depois de **verificada a funcionar** — não basta compilar.
- **Backend:** o endpoint responde (curl ao `/health` e à rota nova); a migração aplica sem erro. Funções de tool use testáveis **por texto** antes de haver voz.
- **Frontend:** `npm run build` passa e o fluxo principal funciona no browser. Câmara/microfone testam-se em **dispositivo real** (sobretudo iOS/Safari).
- **Testes:** lógica pura em `backend/test/*.test.mjs` (correm no PC, são **GATE do deploy**). Testes que tocam BD/LLM têm sufixo **`.bd.test.mjs`** e só correm no servidor.
- **GOLDEN SET da classificação** (`test/golden_grupos.test.mjs` + fixture): casos reais com ouro auditado, gate do deploy. Mudaste vocabulário em `categoria.js`? O teste mostra o DIFF. Se intencional: `backfill_grupos.mjs` no servidor → regenerar fixture (`scripts/gerar_golden_grupos.mjs`, ver cabeçalho) → **commitar fixture e código juntos**.
- Se algo não dá para testar automaticamente, di-lo e descreve como verificaste à mão. **Nunca marcar "feito" com teste a falhar ou por correr.**
- **Higiene de dados de teste:** os e2e correm contra a BD REAL (single-user, sem staging). Remover SÓ os ids que o próprio teste criou; nunca `limpar`/`DELETE` global; confirmar no fim que o estado real ficou intacto.

## Deploy / backup / rollback
- **Deploy:** `bash scripts/deploy.sh` (`-f` se o frontend mudou) depois de commit+push. Faz `node --check`, corre os testes puros (GATE), recusa se houver commits por enviar, etiqueta a versão (`git tag vX.Y.Z`), e no servidor: pull → build → restart → espera `/health`. Não repetir o ritual à mão.
- **Backup:** `mysqldump` diário às 04:10 (`scripts/backup_db.sh`; local 14d + off-site Cloudflare R2 encriptado gpg, 90d, teto 5 GB). Correr à mão antes de migração arriscada. Detalhe e recuperação: comentários do script.
- **Rollback:** `bash scripts/rollback.sh <tag|hash>` (`-f` c/ frontend) — repõe o clone do servidor numa ref + restart + health. Migrações são aditivas por regra (não fazem rollback).

## Git
Fluxo conforme o caso (commits pequenos quando ajuda; agrupar quando faz sentido). Mensagens claras, em português. **`git push` é livre.** Só reescrever histórico partilhado é paragem obrigatória (ver acima).

## Documentação — fontes de verdade
Mantém atualizados **após cada alteração que mude o que neles está** (não a cada commit trivial):
- **`Conceito_Historico_Precos_Voz.md`** — conceito, arquitetura, decisões abertas/fechadas (§10).
- **`Schema_e_Funcoes_ToolUse.md`** — schema da BD + contrato das funções/módulos.
- **`Normalizacao.md`** — normalização atual (problema, dificuldades, solução, lacunas).
- **`Taxonomia_Produto.md`** — modelo-alvo facetado (Produto Mestre); o norte da migração.
- **`Classificacao_Fusor.md`** — desenho do **fusor de categoria** ("fichaEan da categoria"): árvore NOSSA (não OFF) + sinais (EAN país/empresa/vizinhos, marca-IDF-por-nível, VLM-pacote, CLIP, OFF-por-EAN, nome) pesados pela fiabilidade MEDIDA; LLM offline (ponte categoria→nó + árbitro). 3 casos reais (Pérolas/Nesquik/anchovas). Inclui o **prompt do VLM a melhorar** (ler+julgar).
- **`Analise_Fontes_Normalizacao.md`** — fontes (números reais) + plano v2 (fases A/B/C/**D=classificação por catálogo**).
- **`Visao_Multi_Pais.md`** — direção multi-país (Europa) + **arquitetura de 2 camadas**: IDENTIDADE universal (EAN, partilhada, enriquece com cada país) vs PREÇO+LOCALE (por país, parametrizável). Princípio **locale-ready, não locale-completo**.
- **`Metodologia_Descoberta_Fontes.md`** — playbook repetível p/ achar fontes locais ao entrar num país: SEMENTE→DESCOBRIR→CLASSIFICAR→SONDAR→MEDIR→COLHER + taxonomia de fontes + cheatsheet de plataformas (PrestaShop/Shopify/SFCC/transfer-state). Exemplo validado: Brasil (Cosmos/Bluesoft = registo-EAN jackpot).
- **`Auth_Zitadel.md`** — serviço de autenticação partilhado (Zitadel IdP OIDC self-host): infra, IDs/endpoints, integração do BigBag (PKCE + JWT + allowlist), modelo multi-app, e o que falta (Google login).
- **`Visao_Conselheiro_Saude_Alimentar.md`** · **`Vertical_Espanha_Mercadona.md`** · **`Paper_Resolucao_Produtos_Talao.md`** · **`Aula_Classificacao_Produtos.md`** + **`Aula_Matching_Produtos.md`** (didáticos: arrumar vs. identificar produto, esp. sem EAN) · Runbook de bootstrap.

## Internacionalização (i18n)
- **Visão multi-país (dono, 2026-06-13):** o app não é só PT. Separar **IDENTIDADE universal por EAN** (partilhada entre países) de **PREÇO+LOCALE por país**. Alimentar a 1.ª (agnóstica), manter a 2.ª parametrizável — **locale-ready, não locale-completo** (ver `Visao_Multi_Pais.md`). Não construir a maquinaria multi-país agora; só parar de cravar PT mais fundo onde a versão por `locale` custa o mesmo.
- **Camada LOCALE concretizada (2026-06-16):** migração 062 `usuario` (email PK, `pais`, `locale`); `config.paises` PT/BR (moeda/símbolo/`fontesPreco`); `resolveLocale` no `auth.js` (cacheado) → `req.user` ganha país/locale/moeda; o `/info` devolve o preço de catálogo na moeda do país (R$/€); seletor de país no menu do avatar. **Entrada no BR:** ~11 mercados VTEX + Pão de Açúcar (GPA/Linx) + Condor. Carrefour BR bloqueado (Cloudflare). Detalhe em `Visao_Multi_Pais.md`.
- **Base PT-BR**, tratar o usuário por "você". **Nunca hardcodar texto visível** — incl. o gerado por LLM (os prompts pedem PT-BR + "você").
- **Frontend:** todo o texto da UI por `frontend/src/i18n.js` via `t('chave', vars)`. Traduzir = adicionar um dicionário. O `/admin` está fora do âmbito i18n.
- **Resolução do nome de um EAN é PT-first** (catálogo PT > OFF traduzido; marcas/nomes próprios nunca se traduzem; nomes PT-PT não se abrasileiram). Detalhe no Conceito.
- **Exceções (dados, não UI):** prompts internos de extração e nomes de produtos canónicos.

## Notas técnicas
- IA toda via **OpenRouter**, chave própria do BigBag (`OPENROUTER_API_KEY`, com limite de gasto). Áudio em base64.
- **Extração = `gemini-2.5-flash`** (`OPENROUTER_MODEL_EXTRACAO`); verificação = `gemini-3-flash-preview`. Custo é não-fator (<$0,01/nota); o barato perde itens.
- **~97% do custo é a ingestão** (metade leitura-VLM, metade normalização item-a-item). A alavanca é o **catálogo determinístico** (item resolvido sem LLM corta canonicalizar+mestre). A aba **Custos** mede por feature.
- Comparações de preço usam sempre `preco_por_base` (€/kg, €/L, €/un); filtrar `is_clearance`/`is_non_product`.
- **Preço de catálogo (online) é só referência, NUNCA critério** (dono, 2026-06-11): no matching entra só como bónus de desempate; o histórico de preços vem exclusivamente dos talões (facto).

---

## Estado atual (2026-06-16 · fase BETA)

**Superfícies** (routing por path em `frontend/src/main.jsx`):
- **App de chat (`/v1`)** — PWA antiga, **CONGELADA** (era o default em `/` até v0.0.156.0; o default passou a ser a v2). Notas (📷 câmara inteligente: barras→produto, senão→talão), perguntas, **lista/carrinho partilhada**, scanner, **despensa** (049), gastos, "por identificar", perfil nutricional. Produto → ficha factual + avaliação personalizada. Base LOCAL no telefone (IndexedDB) p/ scan instantâneo/offline. **Despensa INDEPENDENTE da lista (v0.0.149.0, decisão do dono):** ícone próprio no topo (armário, cor âmbar) com pílula de contagem — saiu do kebab; entrada SÓ pela tela de despensa (scan), já NÃO pelo scan→lista (que enchia a lista do que já se tem); mesmo formato rico da lista (`GET /despensa` reusa `resolverItensLista` no modo **`leve`**: secção, marca, tamanho, preço-facto + validade — salta a estimativa de preço pelo irmão e o disparo do peso-VLM, que só servem p/ DECIDIR a compra). **Perf:** índice `catalogo_produto.marca` (054), tokens dos SKUs pré-computados em `carregarSkus`, `marcaDeterministica` memoizada → GET de ~6,5s p/ ~2,5s; **scan→despensa OTIMISTA** (insere o item + realce/scroll na hora, secção por `grupoDeNome` no cliente, reconcilia em fundo); open com cache pré-aquecido no mount.
- **Operador (`/admin`)** — desktop. Abas: Painel · SKUs · Mestres · Ligar nomes · Nomes · EANs · Mercadona · Itens · Fichas · Revisão · Qualidade · Preços · Saúde · Uso · Custos.
- **Comprador (`/explorar`)** — desktop, tema "talão": explorar produtos, preço pago vs €/base, por mercado.
- **App v2 (`/` — DEFAULT, alias `/v2`)** — design "cartoon" (claro/lúdico, PT-BR), agora a versão **principal** (v0.0.156.0). Mesmas funções da v1, reusa `api.js`/`i18n.js`/`marca.js`. Vive em `frontend/src/v2/` (`AppV2.jsx` + `cartoon.css` + `icons.js`/`brand.js`), **CSS escopado em `.v2`** (a v1 está em `.bb` → zero colisão). **Nav inferior** Início · Lista · **[scan central elevado]** · Histórico · Perfil. Telas ligadas ao backend: Início, Lista, Histórico, Ficha (réguas semáforo + **pílulas de nutrição nas alternativas**), Comparar, Consultar (código/foto/voz/texto), Despensa, Minhas compras/Gastos, Talão, Perfil, Receitas. Backlog do design nas memórias.

**Pipeline (ASSÍNCRONO desde 2026-06-27):** `POST /api/faturas` (câmara/galeria · ficheiro · Share Target) guarda a imagem + cria um **job `em_analise`** (`fatura_job`, migr. 092) e **responde já (202, ~24ms)** — o utilizador não fica preso. Um **worker em fundo** (`ingest/filaNotas.js` `processarJob`/`varrerPendentes`; loop no `server.js`) corre o pipeline (movido p/ `ingest/processarNota.js`, intacto): VLM-imagem ou texto-PDF+LLM → extração c/ auto-correção (reconcilia total) → dedup → normalização (`preco_por_base`) → canonicalização+matching → verificação de nomes → fecha o job em **pronto / precisa_revisao / falhou**. Frontend v2 (`Notas`): cartão "Em análise" otimista → **polling** `GET /api/faturas/jobs` (3,5s) → vira talão lido sozinho; `falhou` → "Não consegui ler" + `POST /jobs/:id/retry`. Tolerância a falhas: `lease_em`+`tentativas` (job preso após restart → re-apanhado pela varredura; ≥3 erros → `falhou`). Notificação **só in-app (polling), sem Web Push** (decisão do dono). v1 (congelado): mostra "em análise", o talão aparece em Minhas compras. **Consulta:** tool use (`POST /api/consulta` texto, `/api/voz`).

**Infra FECHADA:** Apache+Let's Encrypt, systemd porta 4200. BD `app_bigbag`, **migrações até 092** (092 = `fatura_job`, fila do talão ASSÍNCRONO — ver Pipeline; 085-091 = receitas/foto/imagem/idx, ver `migrations/`; 084 = `programa_sinal`, sinal QUALITATIVO de programa de laboratório por EAN×fonte (postura 2): VLM só detecta existência+nome do programa (nunca preço/percentual), validação obrigatória (estruturado/texto→VALIDADO; só-VLM→≥2 capturas), expira_em=ultima_confirmacao+14d, percentual_indicativo NULL (porta p/ regras curadas); só schema, captura/cron/UI = tarefas seguintes; 083 = `alerta_log.preco_cond`+`preco_cond_fonte`+`preco_cond_obs`, PBM informativo no alerta do Trilho A — o motor de gatilho dispara só sobre TABELA, captura o condicional p/ informar; 082 = `usuario_monitor`+`alerta_log`, monitor de preço GLP-1 POR USUÁRIO — schema do Trilho A: baseline declarado/sugerido, gatilho limiar 8% E piso R$80, FK→usuario(email) CASCADE p/ exclusão LGPD; motor de gatilho+push = tarefas seguintes; 081 = `medicamento_curado`, override curado de força clínica+qtd da classe GLP-1 (Cluster 2, aditivo; a equivalência lê COALESCE(curado,medicamento)); 080 = `medicamento_monitor_hist.preco_cond`, PBM no histórico do monitor; 079 = `catalogo_produto.preco_cond`/`preco_cond_obs`, preço condicional (desconto do laboratório/PBM); 078 = `medicamento_monitorado` + `medicamento_monitor_hist`, monitor 4/4h de preço+estoque dos remédios monitorados — ver §Medicamentos; 077 = `medicamento_explicacao`, cache do "para que serve" LLM; 076 = `anvisa_registro` + `medicamento.categoria_anvisa`/`principio_ativo`, enriquecimento oficial ANVISA Dados Abertos — ver §Medicamentos; 075 = `medicamento`, vertical de remédios BR — ver abaixo; 074 = `catalogo_preco_hist`, histórico de preço do catálogo append-only — ver harvest; 073 = `lista_item.unidade`/`qtd_medida`, quantidade por peso; 072 = `nutricao_usda`, USDA SR Legacy/FoodData Central — ~6,5k alimentos GENÉRICOS por 100g, DOMÍNIO PÚBLICO, 3.º nível do matcher (cauda longa); 071 = `nutricao_fao`, FAO/INFOODS uFiSh+uPulses — 100 alimentos globais por 100g (peixes+leguminosas), COMPLEMENTA a TACO (tapa pescados/leguminosas que a TACO cobre mal), licença CC BY-NC-SA permite uso não-comercial; 070 = `nutricao_taco`, TACO/NEPA-UNICAMP — 591 alimentos GENÉRICOS BR por 100g, nutrição AUTORITATIVA grátis p/ arroz/feijão/frango/frutas que o OFF/retalho BR não cobre; 069 = `catalogo_produto.pdp_em` (timestamp do enriquecimento PDP Pão de Açúcar); 068 = `base_local.seq` AUTO_INCREMENT, sync por seq monotónico p/ apanhar crescimento vivo; 067 = `base_local` + `base_local_evento`, base de identificação+nutrição pré-construída p/ o telefone + telemetria hit/miss; 066 = `perfil_membro.saude_estado` JSON, editor de saúde; 065 = `fila_vetorizar`, fila de vetorização em fundo; 064 = FULLTEXT `catalogo_produto(nome,marca)`; 063 = `ean_empresa`, prefixo→marca; 062 = `usuario`, país/locale; 061 = `categoria_ancora`; 060 = `marca_perfil`; 059 = `produto_busca`; 058 = `catalogo_produto.product_type`; lista no `Schema_e_Funcoes_ToolUse.md §1d`). Migrações novas: `mysql … < ficheiro` (ou via node) no servidor (aditivas por regra). *(Nota: `off_full` foi criada por script, não por migração numerada.)*
- **Coluna JSON (mysql2 devolve OBJETO, não string):** usar SEMPRE `parseJsonCol` de `db.js` — nunca `JSON.parse` cru sobre valor da BD (rebenta). Fonte única.

### Autenticação (detalhe em `docs/Auth_Zitadel.md`) — Google login FECHADO (2026-06-15)
Serviço de auth **próprio e partilhável entre apps** (não SaaS por-MAU, não auth à mão): **Zitadel** (IdP OIDC) self-host em Docker no host, atrás do Apache em **`https://auth.hal9klabs.com`** (Let's Encrypt). BigBag = **client OIDC** (PKCE). Backend (`auth.js`) valida o **access token JWT** (JWKS+issuer, `jose`) e exige email na **allowlist** (`AUTH_ALLOWLIST`); HTTP Basic/`ENABLE_TEST_AUTH` fica de fallback. Frontend (`auth/oidc.js`, `oidc-client-ts`): botão "Entrar" → Zitadel, rota `/callback`, Bearer. **Login GOOGLE a funcionar:** IdP Google na org BigBag (ativado), e o utilizador **regista-se sozinho** pelo Google (External User Not Found → **Register** → ele cria a conta do email verificado, ativa na hora — **NÃO pré-criar** users: sem SMTP, ficam `INITIAL` por ativar e travam; lição de hoje). A allowlist (por email) é que dá acesso ao BigBag. **`auth.js` NÃO envia `WWW-Authenticate: Basic` no 401** (senão o browser abria o diálogo nativo e tapava o login OIDC). Multi-app: cada app = um client. **Nome na saudação (2026-06-16):** o `/api/me` devolve `nome` (given_name do Google, das claims ou /userinfo, cacheado); a v2 saúda pelo nome (fallback ao local-part do email, nunca o email inteiro). **Falta:** rotacionar o PAT (exposto); SMTP no Zitadel (reset/verify por email).

### Sistema de classificação (o eixo desta fase — detalhe nos docs-fonte)
- **Duas lentes** (dono, 2026-06-12): **de loja** (`it.grupo`, o corredor — segue como as lojas organizam; massa/arroz/cereais em *mercearia*) serve comparar/comprar; **da lista** (seção de exibição) é o cabeçalho da lista de compras. São eixos distintos de propósito.
- **`grupoDe`** (`normaliza/categoria.js`, módulo PARTILHADO front/back, puro): ordem dos sinais = congelados(loja) → **NOME** (nosso vocabulário) → food_groups OFF → categoria-loja. Invertida em 2026-06-13 (NOME antes do OFF crowdsourced); a ordem antiga contaminava o ouro do golden.
- **Resolvedor ÚNICO da ficha por EAN** (052, `normaliza/fichaEan.js`): a ficha é a FUSÃO campo-a-campo de TODAS as fontes locais, com **tabela de prioridades num só sítio**. Proveniência por campo + divergências em `produto_ean.fusao`; 'manual' sagrado; nutrição catálogo-oficial>OFF>VLM; ingredientes = o mais completo (anti-OCR, anti-estrangeiro); idempotente (testado). Backfill `refundir_fichas.mjs` aplica+regista diff.
- **Classificação por catálogo** (`normaliza/classificarCatalogo.js`, Fase D): as linhas de catálogo votam (EAN direto, ou ~80 vizinhos por nome), peso = profundidade do caminho, vencedor por **FAMÍLIA** (2.º nível). Seção da lista = tipo curado saliente > família (`cat_exib`) > grupo (3 iterações: corredor grosso, folha fina, família certa). Guardas anti-colisão de EAN, raso-não-vota, ES-não-exibe. Avaliador: `scripts/avaliar_classificacao_catalogo.mjs`.
- **Nome:** "à talão" (genérico da secção cortado; marca à parte noutra cor); quantidade embutida sai (`cortarQuantidadeNome`: "20 Saq", "… Saquetas"); verificação de nomes (037, voto a 3).
- **ALIMENTO vs NÃO-ALIMENTO** (`normaliza/tipoProduto.js`, puro+testado): determinístico (nutrição/food_groups → padrões de não-alimento no NOME, fiáveis mesmo c/ categoria errada → categoria loja/OFF PT+ES → alimento-sem-nutrição água/vinho/especiarias). Guardado em `catalogo_produto.product_type` (058, backfill `scripts/backfill_product_type.mjs`: ~65% determinístico) + LLM p/ os ambíguos (`scripts/classificar_ambiguos_llm.mjs`, lotes de 100, ~$1). O `/info` devolve `tipo`; a ficha v2 esconde Para-Sue/nutrição em não-alimentos (layout simples marca/categoria/tamanho).
- **Auditoria mensal do scan:** `node scripts/auditar_grupos.mjs --scan` (juíz calibrado por canários) 1×/mês ou após sessão grande; adotar termos claros, regenerar golden no mesmo commit.

### Identificação por foto + EAN + marca (NOVO, 2026-06-16 — detalhe em `Classificacao_Fusor.md`)
- **Análise do EAN (passo 0):** `normaliza/ean.js` (`paisDoEan` por prefixo GS1, `eanValido` checksum, `analiseEan`); migração 063 `ean_empresa` (prefixo-8 → marca dominante minada). O `/info` devolve `analise_ean`; a marca da empresa entra no voto quando `share≥0.8` + pista ao VLM.
- **Routing do scan:** o `/info` expõe `ficha_magra` (sem nutrição NEM imagem); o scan de consulta com ficha magra **pede FOTOS** (VLM) em vez de abrir ficha inútil — mesmo que haja um nome/marca só decodificado do EAN. A dica de captura sugere fotografar a **tabela nutricional** (verso).
- **ÁRBITRO MULTIMODAL de nome/marca** (`arbitrarMarcaNome`, `ingest/produto.js`): OFF e VLM discordam no nome/marca → um VLM **olha as FOTOS** do pacote + os dois textos e decide. Entra na fusão (`fichaEan.js`, `extra.arbitro`) **abaixo do catálogo curado, acima de OFF/VLM**. Só corre no conflito. (Ideia do dono; resolve trocas/genéricos em qualquer língua — caso chucrute ALDI.) `extrairProdutoFotos` ganhou **retry 2x** (VLM às vezes devolve JSON malformado).
- **Guard `marcaEhTipo`** (`categoria.js`): rejeita uma marca que é um TIPO/genérico PT-ES (Leite/Iogurte mal-metidos no campo marca), single-token. Genéricos estrangeiros (Sauerkraut) ficam p/ a blocklist por rácio (backlog).
- **Gémeo:** gate de precisão da sugestão a 3 sinais (2026-06-17): (1) `nomeCondizGemeo` (nome/termos condizem, não só a marca); (2) **família tem de bater** (só rejeita quando ambas conhecidas e diferem — Páprica/especiarias ≠ Milho/conservas) e **nunca em não-alimento** (Filtros-de-Café ⇏ cápsulas); (3) **confirmação por IMAGEM (CLIP)** — `confirmarGemeoPorImagem` compara a foto do utilizador (produto_foto) com a imagem do candidato; a **foto do candidato só se mostra com `confirmada_imagem`** (score≥0.72), senão fica escondida (sem foto de referência, ex.: scan de código → escondida). `adotarNomeRef` + imagem do `/info` lêem off_full **E** catálogo (gémeo só-BR adota-se); o `gemeo` do `/identificar` ainda é descartado no frontend (Fase 2 pendente).
- **Corpo de imagem cresce sozinho:** `upsertVetores` persiste no Qdrant os vetores baixados on-demand (id = `uuidDoEan`, UUIDv5 — não `Number(ean)`, que colidia); **fila de vetorização em fundo** (migração 065 `fila_vetorizar`, `ingest/filaVetor.js`, worker no `server.js`) — "falha hoje, acerta amanhã" para os downloads OFF que dão timeout. Detalhe em `Exploracao_Match_Por_Imagem.md`.

### Base local no telefone — pré-construída + medida (NOVO, 2026-06-17)
Réplica de **identificação+nutrição** no IndexedDB para o scan responder **instantâneo/offline**, agora também na **v2** (antes só a v1 usava a base local). Decisão do dono: **"começar com menos, medir a taxa de hits, ajustar"**.
- **`base_local`** (migração 067, materializada por `scripts/build_base_local.mjs`, idempotente): fusão **offline** (sem LLM/live) de catálogo PT (`auchan/continente/pingodoce/nutripedia`) + **Mercadona ES** + **EANs portugueses do `off_full`**, com nutrição (catálogo>off_full, shape `{sal,fibra,gordura,acucares,hidratos,proteina,energia_kcal,gordura_saturada}`). **~63k fichas** (46% com nutrição), ~37 MB no telefone. **LIDL/ALDI ficam DE FORA de propósito** — a telemetria mede o que se perde.
- **Sync:** `GET /api/produto/base-local-fichas?desde_seq=` (incremental por cursor de **`seq` AUTO_INCREMENT**, migr. 068). `baseLocal.js` → `sincronizarFichasBulk` escreve na store `fichas`. `Shell` pré-carrega no mount.
- **Local-first (v2):** `aoCodigo` (scan) consulta `fichaLocal` ANTES do servidor → resolve instantâneo/offline nos hits; a `Ficha` renderiza a base local enquanto o `/info` resolve. **NOME ESTÁVEL (2026-06-17):** ao chegar o servidor, a `Ficha` PRESERVA o nome/marca já mostrados da base local (`{...r, nome: prev.nome}`) — é o mesmo produto; o servidor enriquece nutrição/imagem/parecer, não troca a identidade. Sem isto o nome "piscava" (base→servidor) e parecia outro produto. **Offline-first corrigido (code-review 2026-06-17):** um ERRO do `/info` já NÃO marca o servidor como respondido nem tapa a base local → offline mostra mesmo a ficha cacheada (antes caía no ecrã de erro). A lista/comparar só usa o nome local quando é **PT-fiável** (origem `pt_cat`/`uso`); `merc_es`/`pt_off`/`uso_es` caem no caminho do servidor (que traduz) — a lista guarda o nome verbatim, não pode levar nome estrangeiro.
- **Tela "Consultar produto" (scanner) (2026-06-17):** mostra a **régua de navegação** (Início·Lista·scan·Histórico·Perfil) no modo consulta default (não nos fluxos de tarefa: identificar linha/lista/comparar, que voltam com Voltar). O botão **Código sai** da barra de métodos (o scan vem do central da régua, que reabre sempre no modo código via `k=Date.now()`); a barra fica **Foto·Voz·Texto** (o "Produto" passou a "Foto").
- **Telemetria hit/miss:** `POST /api/produto/local-hit` → `base_local_evento` (resolvido-no-telefone vs servidor). `registarHitLocal` no scan (hit só com nome local utilizável). Relatório: `scripts/taxa_base_local.mjs` + **aba `/admin` "Base local"** (`GET /api/admin/base-local`: taxa, tamanho por origem, por contexto/dia, top misses cruzados com off_full).
- **CRESCE COM O USO (dono, 2026-06-17):** todo o EAN resolvido no servidor (um MISS) entra na `base_local` PARTILHADA com nutrição/ingredientes → vira HIT na próxima sync de TODOS. `consultarOuGuardar` faz `upsertBaseLocal` **DEPOIS da tradução** (com o nome PT — senão a base ficava com o nome estrangeiro p/ todos) e **SEMPRE, mesmo em cache-hit** (senão o corpus já-resolvido nunca povoava a base). **Origem `uso`** só quando o nome é PT-fiável (`!pareceEstrangeiro`); tradução falhada → **`uso_es`** (fica fora do atalho da lista até traduzir). Sync por `seq` porque o cursor por ean não descia inserções vivas com ean menor. **Rebuild** (`build_base_local.mjs`) é idempotente por upsert (sem TRUNCATE) → preserva as linhas `uso`. **PROMOÇÃO (2026-06-18, corrige bug):** o `ON DUPLICATE` preservava a origem do bootstrap → um EAN `pt_off`/`merc_es` resolvido+traduzido ficava preso a cair SEMPRE ao servidor (só **6** linhas `uso` em 63k!). Agora `upsertBaseLocal`, quando o nome é PT-fiável e a origem não é `pt_cat`/`uso`, **apaga+reinsere** a linha como `uso` → ganha `seq` NOVO (AUTO_INCREMENT) e RE-SINCRONIZA p/ os telefones → o 2.º scan do EAN é HIT local instantâneo. **Scan→lista** salta o `/info` (só `/consultar?pt=1`, 1 round-trip). **Backfill FEITO (2026-06-18):** `scripts/backfill_promover_base_local.mjs` (--so-pt promove os PT-fiáveis) + `scripts/backfill_traduzir_base_local.mjs` (traduz-ou-mantém os Mercadona/estrangeiros; o `pareceEstrangeiro` é regex estreito e deixava passar espanhol geral, por isso identificam-se os Mercadona pela `catalogo_produto.fonte`). Resultado: base_local **100% PT-fiável** (44.679 `pt_cat` + 18.458 `uso`, 0 `pt_off`/`merc_es`) → todos os 63k são hit local instantâneo.
- **Backlog do code-review** (achados de menor severidade, por fazer): bulk-sync sobrepõe `analise`/`nutricao_confirmada` das fichas ricas da v1 na store partilhada `fichas`; `uso` não grava `product_type`/`nutriscore`/`nova` (não-alimentos piscam layout de alimento até ao `/info`); consulta local-first abre ficha em vez de pedir fotos numa ficha magra; a aba admin não conta o balde `uso`/`uso_es` no breakdown por origem; build pass-1 sem COALESCE pode anular `product_type`/nutrição de uma linha no rebuild; sem recuperação de um RESET do `seq` (versao morta).

### Perfil de saúde — editor estruturado (NOVO, 2026-06-16 — detalhe em `Visao_Conselheiro_Saude_Alimentar.md`)
Editor v2 (`PerfilSaude`) com **9 grupos de pílulas** (Objetivos·Condições·Dieta·Preferir·Evitar·Metas·Suplementos·Medicação·Atividade física), 3 abas (Ativas/Desativadas/Adicionar), catálogo de sugestões, **texto livre (Notas)**, **importar de texto**, múltiplos membros + demografia. As ATIVAS são os arrays do `resumo` (que a avaliação lê); inativas+demografia em `perfil_membro.saude_estado` (066). O perfil vai ao prompt como **bloco etiquetado** (`perfilParaTexto`) — nomeia todos os grupos, linha **`Pessoa`** (demografia; email fora), une metas+nutrientes. Tela Perfil = só o avatar → abre o editor. O parecer **re-corre** quando a nutrição chega (bug corrigido).

### Lista de compras (detalhe no Schema §lista_item)
Servidor é fonte de verdade, sync por polling, cores por membro, reconciliação com o talão. **voz→lista (v2) OTIMISTA (2026-06-17):** assim que a transcrição (`/api/voz/lista`) volta, os itens ditados aparecem JÁ (secção pelo nome via `grupoDeNome`, no cliente) e reconciliam com a lista RESOLVIDA que o `/lista/lote` DEVOLVE — uma só ida ao servidor a seguir (antes: `adicionarListaLote` + `carregar`, dois `resolverItensLista`, ~3s + toast "Adicionei"). Sem aviso de confirmação. A consulta por voz já ia direto à ficha. Sugestões por cadência + cartão de refeições (única chamada LLM, cacheada). ETag/304, Desfazer-o-esvaziar, **outbox offline completo** (`listaOutbox.js`, coalesce). **Cadeia de preço (v0.0.147.0):** FACTO por nome→SKU **e por EAN** (`item.ean` do talão + `produto_ean.item_id` da identificação — caso Picles-Aldi) → "~online" (menor catálogo do EAN) → "estimado" em 3 níveis (irmão→primo→primo-família, dieta nunca relaxa) → nada (honesto). **Quantidade por PESO derivada do histórico (v0.0.185.0, migr. 073 `lista_item.unidade`/`qtd_medida`):** metade de uma compra é a peso ("2 kg batata", "300 g fiambre"); o modo de venda **descobre-se dos talões do próprio user**, não se pede a unidade (decisão do dono). Gate no `resolverItensLista`: SKU `unidade_base='kg'` com ≥2 linhas (e ≥60%) a trazer `linha_peso` (balcão) ou `peso_em_falta=1` → `it.unidade='kg'` + `it.qtd_medida`=peso habitual (`preco_liquido/preco_por_base`) ou 0,5 kg; líquidos (L) ficam contagem (embalados). Stepper de peso na v2 (passo 0,05/0,25 kg), `fmtMedida` (g/kg), total = €/base × qtd_medida; PATCH grava o override do user. **33 SKUs detetados (validado nos talões reais); ~3 falsos-positivos aceites pelo dono ("deixar como está", 2026-06-18)** — secos embalados kg-preçados (Arroz Agulha, Iogurte Grego Ligeiro, Muesli) entram por `peso_em_falta` e o user ajusta no stepper. **Refinamento ADIADO:** corroborar `peso_em_falta` com o `grupo` do SKU (fresco/talho/charcutaria a peso; mercearia/laticínios-embalados a contagem).

### Medicamentos (Brasil) — comparação de preço (NOVO, 2026-06-22)
Vertical de remédios BR, **foco = comparação de preço**. Reaproveita a arquitetura "IDENTIDADE universal por EAN vs PREÇO por país" e ~80% da infra existente.
- **IDENTIDADE** (`medicamento`, migr. 075, keyed por EAN): alimentada pela **lista CMED/ANVISA** (o registo-jackpot público — substância/princípio ativo, apresentação, laboratório, tipo referência/genérico/similar, e os tetos legais **PF**/**PMC** por ICMS). `scripts/carregar_cmed.mjs` descobre o xlsx mensal mais recente em gov.br, deteta o cabeçalho, expande multi-EAN (até 3/registo), upsert idempotente. Carga atual: **26,3k EANs · 2,2k princípios ativos · 9,6k genéricos**.
  - **Enriquecimento OFICIAL ANVISA (migr. 076, 2026-06-22):** o nº de registro ANVISA codifica a identidade — `1.YYYY.WWWW.XXX-Z`, os **9 primeiros = PRODUTO**, os 3 seguintes = apresentação. Logo o mesmo remédio (vários tamanhos) partilha `LEFT(registro,9)`; isto arruma o **dedup da busca** E a **foto** (EAN irmão). Carreguei o **Dados Abertos ANVISA** (`DADOS_ABERTOS_MEDICAMENTOS.csv`, 43k linhas, latin1, sem EAN) → `anvisa_registro` (por registro-9) e enriqueci `medicamento` via `LEFT(registro,9)` (**99,7% match**): `categoria_anvisa` (Genérico/Similar/Novo/Específico/Biológico/Fitoterápico) + `principio_ativo`, e o flag `generico` passou a ser **ANVISA-certificado** (9.583, confirma a CMED). O servidor NÃO alcança a ANVISA → baixar no PC + `scp` (ver cabeçalho de `carregar_anvisa.mjs`). A ANVISA não tem "Referência" (são "Novo") → o display do `tipo` fica o da CMED (mais amigável); `categoria_anvisa` exposto à parte. NÃO há mapa genérico↔referência para baixar (a intercambialidade migrou p/ consulta online).
- **`normaliza/medicamento.js`** (puro, 13 testes): `parseApresentacao` → dose/forma/qtd (99% dose, 99,5% forma; apanha combos "(600+60+60) MG"); `precoPorDose` (R$/comprimido = o `preco_por_base` dos remédios); `chaveEquivalencia` (substância+força+forma).
- **PREÇO** = farmácias online pelo MESMO pipeline `catalogo_produto` (UPSERT) + `catalogo_preco_hist`. **11 redes** em `fontes_farmacia.json` (campo `motor`); o `routes/medicamento.js` lê a lista de fontes DESTE manifesto (fonte única — acrescentar farmácia = editar o JSON + colher):
  - **15 VTEX** (`harvest_vtex.mjs`, subárvore Medicamentos por `--cats`): paguemenos, saojoaofarmacias, extrafarma, drogariavenancio, drogal, drogasmil, drogariaglobo, drogariarosario, farmaciaindiana, catarinense, farmais, farmaconde, drogariamoderna, farmagora, precopopular (~2,4–2,5k SKUs cada).
  - **Pacheco + Drogaria São Paulo** (DPSP, VTEX, cats Medicamentos=800): GEO-bloqueadas (CloudFront só-BR) — colher com `--proxy`. **Proxy BR = Lightsail São Paulo** (tinyproxy `54.20.28.232:8888`, restrito ao IP `85.25.46.6`, BasicAuth; `PROXY_URL` no `.env`). `src/rede.js` (undici ProxyAgent, SÓ no processo de colheita com `--proxy` → rede do host intacta). Aceder a site público a partir do país onde é servido NÃO é evasão de bot-detection. **Ultrafarma** também é só-geo (200 pelo proxy), mas é plataforma PRÓPRIA (não-VTEX, tem JSON-LD) → precisa de adaptador próprio (parqueada).
  - **RaiaDrogasil** (drogasil — a GIGANTE, líder de mercado; não-VTEX, Next.js/Cloudflare): `scripts/harvest_raiadrogasil.mjs` colhe DIRECIONADO por EAN via **schema.org JSON-LD** (`search?w=<EAN>` → PDP → `gtin13`+`offers.price`), os dados estruturados que o site publica para crawlers. Polido (700ms), aborta se o host se defender; NÃO resolve CAPTCHA/desafio. (drogaraia = mesma plataforma, falta ligar; onofre = stack RD.)
  - **Panvel** (drogariapanvel — #5 nacional, RS/Sul, grupo Dimed): NÃO-VTEX (Angular SSR, API privada BFF). `scripts/harvest_panvel.mjs` colhe por EAN pela API **pública** `POST /api/v2/search?type=CSR&uf=03` (a **v2 dispensa cookies de WAF**; a v3 exige Azion). Headers genéricos (`app-token: ZYkPuDaVJEiD`, `client-ip`, `sessionid`, `source: mobile`, `user-id: 0`); body `{term:EAN,itemsPerPage,currentPage,assortment:"mais relevantes",filters:[],searchOffers:false,searchType:"term"}`; busca por código de barras exige **match exato** (`totalItems===1`); preço = `discount.dealPrice‖originalPrice` (o `pbm.priceList` é desconto condicional de 1ª compra, ignorado). Sem sinal de estoque (só preço). Módulo partilhado `ingest/precoPanvel.js` (harvester + monitor). 1 req/EAN, sem PDP.
  - **Nissei** (farmaciasnissei.com.br — #6 nacional, PR/SC): NÃO-VTEX (Next.js). `scripts/harvest_nissei.mjs` colhe por EAN: a busca **`/pesquisa/<EAN>` é server-rendered** (dá o slug da PDP; o preço ali é "0,00" hidratado por JS), e a **PDP** traz um bloco **schema.org Product** server-rendered com `gtin`+`price` (JSON inválido → extraído por regex). Fluxo: busca→slug→PDP→`gtin` confirma o EAN (vem em GTIN-14 com 0 à frente)→preço. 2 req/EAN. Módulo `ingest/precoNissei.js` (harvester + monitor).
  - **Araújo** (araujo.com.br — grande rede MG): NÃO-VTEX mas **server-rendered e limpa**. `scripts/harvest_araujo.mjs`: a busca **`/busca?q=<EAN>`** dá o link da PDP (`/<slug>/<id>.html`), e a PDP traz **schema.org Product/Drug VÁLIDO** (`gtin13`+`offers.price`). Fluxo busca→PDP→gtin confirma→preço (2 req/EAN). Módulo `ingest/precoAraujo.js`. **Guard:** o `ld.sku` da Araújo é um OBJETO → usar o id do URL como `sku_fonte` (senão "[object Object]" colide na chave única e sobra 1 linha).
  - **FORA por WAF (não evadir):** DPSP **Pacheco/São Paulo** (CloudFront 403, contornado via proxy BR), **Ultrafarma** (gocache 403) — bloqueio ativo do servidor; entrar = evasão de bot-detection, proibido. Tamoio não-VTEX. (Restantes da cliquefarma: micro-lojas de especiais, sem valor de retalho.)
- **`routes/medicamento.js`** (`/api/medicamento`): `/info?ean=` (mesmo EAN → farmácia mais barata + % abaixo do PMC), `/equivalentes?ean=` (genérico vs marca ordenados por preço/dose), `/buscar?q=` (FULLTEXT). A "vista" = `catalogo_produto ⋈ medicamento` on ean (a junção filtra o catálogo da farmácia para só remédios). **Só informação e preço, NÃO aconselhamento médico.**
- **UI — superfície PÚBLICA "BigBag Remédios" (`/remedios`, SEM login — utilidade pública):** rota própria em `main.jsx` → `RemediosApp` (export de `AppV2.jsx`), tela `Remedios` standalone (header de marca próprio), **separada** do app de compras (não há entrada no Início — "não misturar", dono 2026-06-22). Busca por NOME (autocomplete `/api/medicamento/buscar`, debounce 220ms) OU SCAN (`lerCodigoBarras` → `/info`). Ficha = identidade + MAIS BARATO/farmácia/% abaixo do PMC + preço por farmácia + equivalentes por preço/unidade. **Endpoints `/api/medicamento/*` são PÚBLICOS** (sem `requireAuth`; sem PII). **DEDUP da busca pelo REGISTRO ANVISA:** o nº (13 díg., `1.YYYY.WWWW.XXX-Z`) tem os **9 primeiros = produto**, os 3 seguintes = apresentação → o mesmo remédio (vários tamanhos) partilha `LEFT(registro,9)`. Agrupa por `LEFT(registro,9)+dosagem+forma` (autoritativo, não match de nome; +dosagem porque 35% dos produtos-registo cobrem >1 força), representante = melhor **preço/dose**; a linha mostra "R$X/un". A ficha mostra o registro (`registro_fmt` no `/info`). **Preço/unidade só compara dentro da MESMA apresentação (dono, 2026-06-23):** R$/comprimido ≠ R$/ml ≠ R$/g — nunca ranking cruzando formas. Apresentações da marca ordenam-se AGRUPADAS por forma→dosagem (per-unidade só ranqueia dentro de cada forma+dose); equivalentes exigem substância+dose+forma por **igualdade exata** (não `<=>`, que casava NULL→misturava xarope c/ comprimido); rótulo da unidade real via `unidEmb` (/un,/ml,/g). CSS `.med-*`/`.rx-*` em `.v2`. Aviso "não é aconselhamento médico".
- **Provado e2e (2026-06-22):** grupo Drospirenona+Etinilestradiol — genérico R$1,01/comp vs Yasmin R$4,42/comp (**4,4× por dose**); melhor preço 69% abaixo do teto PMC; mais barato em paguemenos (R$24,29) vs saojoao (R$31,09). **Falta:** PMC pela UF do usuário (hoje usa o teto 18%); tarja/receita (a CMED nem sempre traz); adaptador Ultrafarma (geo-ok via proxy, plataforma própria).
- **GUARD de preço `precoValido` (Cluster 1, 2026-06-23):** `normaliza/precoValido.js` — um valor só é PREÇO se não for sentinela do VTEX (`99999/999999/9999999`, valores EXATOS — há remédio legítimo até R$558k, nunca filtrar por faixa), nem centavo (`<0,50`), nem `<=0`, nem (VTEX) `IsAvailable=false`/`AvailableQuantity<=0`. Aplicado em `harvest_vtex` (causa-raiz, antes sem filtro), `precoVivo` (estoque+ao-vivo, antes só apanhava 7 noves) e os adaptadores panvel/nissei/araujo. **Vertical da fonte:** `normaliza/vertical.js` `verticalDaFonte()` — farmácia se ∈ `fontes_farmacia.json`, mercearia se ∈ `fontes_vtex.json`+{condor,extra,paodeacucar,superprix}, **default desconhecida='outro' (nunca farmácia)**. Limpeza retroativa feita (1.272 linhas sentinela/centavo → NULL em `catalogo_produto`, DELETE em `catalogo_preco_hist`), em transação com tripwire.
- **Harvest ACUMULA — regra dura (dono): UPSERT, NUNCA DELETE+recolher.** As 6 engines respeitam: `harvest_vtex.mjs` (VTEX), `harvest_raiadrogasil.mjs` (RD JSON-LD por EAN), `harvest_panvel.mjs` (Panvel — API BFF por EAN), `harvest_nissei.mjs` (Nissei) + `harvest_araujo.mjs` (Araújo — ambas: busca server-rendered + PDP schema.org por EAN), `harvest_jsonld.mjs` (genérico **sitemap+schema.org JSON-LD** p/ não-VTEX: Magento/Woo/custom — enumera produtos pelo sitemap, lê `gtin13`+`offers.price` de cada PDP). Todas: `ON DUPLICATE KEY UPDATE` (produtos que saem da loja ficam, campos enriquecidos preservados), cada mudança de preço/SKU novo vai ao **`catalogo_preco_hist`** append-only (migr. 074; o preço anterior nunca se perde), EANs novos entram, **GUARD 0-linhas** (colheita vazia não apaga os bons).
- **REMÉDIOS MONITORADOS — histórico 4/4h de preço+estoque (migr. 078, dono 2026-06-23):** remédios numa lista de monitorados (`medicamento_monitorado`, por MARCA; hoje **OZEMPIC + MOUNJARO**) — **e TODOS os seus equivalentes/genéricos** (o monitor expande por SUBSTÂNCIA → semaglutida: Ozempic/Ozivy/Poviztra/Rybelsus/Wegovy/Extensior; tirzepatida: Mounjaro; **26 EANs**, auto-inclui genéricos novos) — têm o preço+estoque colhidos **de 4 em 4 horas** e ARMAZENADOS (`medicamento_monitor_hist`, append-only, 1 linha/ean×farmácia/colheita: `preco`, `disponivel` 1/0, `qtd_estoque`) → histórico denso de variação E de disponibilidade (esgotou? voltou?). `ingest/monitorarPrecos.js` (p/ cada EAN com oferta, consulta TODAS as VTEX ao vivo via `precoEstoqueVtex` — preço+estoque SEM frete/CEP — proxy nas geo; **+ Panvel + Nissei** via `precoPanvelEan`/`precoNisseiEan`/`precoAraujoEan`, só preço, `disponivel=NULL`); cron `scripts/monitorar_precos.mjs`. Leitura: `GET /api/medicamento/monitor?ean=&dias=` (min/máx, % em estoque, série). Acrescentar remédio = `INSERT INTO medicamento_monitorado (produto)`.
- **PREÇO CONDICIONAL — "Desconto do laboratório" (PBM) (dono, 2026-06-23):** remédios caros mostram DOIS preços: o NORMAL (de prateleira) e um mais barato CONDICIONAL (PBM/programa do laboratório, exige CPF/cadastro). O R$999 da Araújo p/ Ozempic era o PBM, não o de prateleira (R$1.314,38) → saía como falso "mais barato". Regra: **`preco` = o NORMAL** (base honesta da comparação); o condicional fica em **`catalogo_produto.preco_cond`/`preco_cond_obs`** (migr. 079) COM o motivo. Capturado onde é PÚBLICO: **Panvel** (`pbm.priceList`+labelMessage) e **Araújo** (`decimalPrice` normal vs JSON-LD=PBM). **Nissei/RD** têm o desconto mas o valor é **gated por CPF** (não capturável); **VTEX** não expõe camada PBM (o `Price` é o real). O `/info` devolve `melhor` (normal) + `melhor_cond` (menor com desconto); a ficha mostra os dois + o motivo. O **monitor 4/4h também grava o `preco_cond`** (migr. 080) → série dos dois preços p/ a classe (PBM = −25% a −59% em Ozempic/Mounjaro/genéricos).
- **"PARA QUE SERVE" em linguagem simples (`/api/medicamento/explicacao?ean=`, migr. 077):** a bula oficial está atrás de WAF Imperva (consultas.anvisa.gov.br dá 403 mesmo via proxy — não evadir). Em vez de raspar, um LLM (gemini-flash) gera "para que serve / como se usa / cuidados" em PT-BR leigo **FUNDAMENTADO no princípio ativo + classe** (não no texto da bula), conservador (manda à bula se incerto), sem posologia. Cacheado por substância (`medicamento_explicacao`) → 1 geração por ativo, reusada por todas as marcas/EANs. A ficha mostra o cartão + disclaimers + **link para a bula oficial da ANVISA** (link-out: o browser do utilizador resolve o desafio Imperva). Informação, não aconselhamento.
- **PREÇO + FRETE AO VIVO (`/api/medicamento/precos-ao-vivo?ean=&cep=`, público):** padrão "cache p/ navegar, ao-vivo no clique" (preço é volátil). Ao abrir a ficha, consulta TODAS as VTEX em paralelo (`fq=alternateIds_Ean` p/ o preço atual + `orderForms/simulation` p/ o **frete** ao CEP), proxy por-pedido nas geo (DPSP). **Verifica ESTOQUE** (`commertialOffer.AvailableQuantity`>0 / `IsAvailable`): farmácia esgotada não entra na lista ao vivo (não adianta indicar preço que não dá p/ comprar). Ordena pelo **TOTAL = produto+frete** (o produto mais barato pode ter o total mais caro), separa **entrega** de **retirada-em-loja** e deteta **"não entrega neste CEP"** (frete-sentinela). ~4s p/ 17 farmácias. Debounce 5 min por (ean,cep); CEP ref **22241-040**. Alimenta o histórico (mudança de preço → `catalogo_preco_hist`). `ingest/precoVivo.js`. A ficha v2 mostra a cache e atualiza ao vivo ("Mais barato com entrega · agora"). **FRETE GRÁTIS acima de X:** 2.ª simulação (carrinho ~R$250) deteta a política; o **valor EXATO** vem da homepage (`scripts/fretes_gratis.mjs` minera "Frete Grátis acima de R$X" → `fretes_gratis.json`, no cron; 8/18 publicam) e tem prioridade sobre a estimativa. Apanha até pedido-mínimo (Pacheco: não entrega 1 un, grátis acima de ~R$258).
- **CRON (colheita periódica, no crontab do `dev`):** **`scripts/refresh_farmacias.mjs`** (orquestrador — corre cada fonte do `fontes_farmacia.json` pela engine certa: vtex `--fundo`, raiadrogasil `--atualizar`, jsonld, `--proxy` se `geo`; sequencial+pausa) **semanalmente (Dom 01:00)**; **`carregar_cmed.mjs` SEMANAL (quarta 01:30)** (a CMED publica-se mensalmente mas re-corre semanal p/ apanhar republicações/correções mais cedo; upsert idempotente); **`monitorar_precos.mjs` de 4 em 4 horas (`0 */4`)** — colhe preço+estoque ao vivo dos remédios MONITORADOS (ver abaixo) e grava histórico denso. O Continente noturno e o backup 04:10 já existiam. *(ANVISA Dados Abertos é manual: o servidor não alcança a ANVISA → baixar no PC + scp.)*

### Outros eixos construídos
- **Eixo saúde:** identificação por EAN (scan/foto/linha do talão) + OFF; ficha factual não-clínica; frescos por nome (`produto_generico`); perfil por membro com alertas de alergia; comparar na prateleira. Ver `Visao_Conselheiro`.
- **Fontes de catálogo ~60k** (`catalogo_produto`): Auchan/Continente (c/ EAN), Pingo Doce/Lidl (sem EAN), Mercadona ES, lidl-fr, mercadona-off. + `off_produto` (~27k, dump antigo). **OFF COMPLETO LOCAL (`off_full`, 2026-06-15): ~4,5M produtos** (3,3M c/ imagem_url, 2,2M c/ nutrição em colunas planas) — importado por DuckDB do dump CSV. `consultarOFF` e a fusão (`fichaEan`) leem o `off_full` como backstop (nutrição+imagem por EAN) ANTES da API live → corta rate-limits. NÃO é superfície de busca por nome (multilíngue); para isso é o `produto_busca` (ver abaixo).
- **TACO + FAO (nutrição de genéricos, 2026-06-17)** (`nutricao_taco` migr. 070 + `nutricao_fao` migr. 071; matcher ÚNICO `nutricaoGenerica` em `normaliza/taco.js`; loaders `carregar_taco.mjs` de `data/taco.json` e `carregar_fao.mjs` de `data/fao.json` — este TRADUZ os nomes EN→PT com LLM no load): **3 níveis: TACO > FAO > USDA.** **TACO** (591 alimentos BR, NEPA-UNICAMP) é PRIORITÁRIA; **FAO/INFOODS** (uFiSh peixes + uPulses leguminosas, 100 alimentos, `extrair_fao.py` dos Excel CC BY-NC-SA) tapa tilápia/camarão/azuki/ervilha; **USDA SR Legacy** (~6,5k genéricos, `extrair_usda.py` do CSV FoodData Central, DOMÍNIO PÚBLICO; itens de marca + categorias US-específicas filtrados) é a cauda longa. O matcher consulta os 3 (cada um no seu try; bónus +0.3 TACO / +0.1 FAO nos empates) e devolve `fonte` (`taco`/`fao`/`usda`). Loaders `carregar_fao.mjs`/`carregar_usda.mjs` TRADUZEM os nomes EN→PT com LLM no load. Em `garantirGenericoSku`, essa nutrição **substitui a ESTIMATIVA do LLM** quando há match de confiança por NOME; senão `null` e fica a estimativa do LLM (grátis, já calculada). Matcher conservador: gate "ficha LIDERADA por um alimento do nome" (mata Ovo→Macarrão-com-ovos, Leite Integral→Canjica), filtro de FORMA (pó/instantâneo/chips não pedida = outro produto), variedade PROTOTÍPICA por frequência (ovo→galinha, batata→inglesa, feijão→carioca), forma-base "como vendido" (cru). 6 buracos do dump (macros todos 0, ex.: leite líquido) excluídos no load. Sem `acucares` (a TACO não traz açúcares).
- **Ferramenta peso-pela-imagem** (`ingest/pesoImagem.js`, 051): VLM lê o peso da imagem do catálogo/OFF; fase 2 via SerpApi (Google CSE bloqueado pela conta IONOS). Lazy a partir da lista, 1×/EAN.
- **Telemetria de uso** (032) + **custos por feature** (`custo_chamada`).
- **Normalização v2 fases A/B** (035-036, 041-042): conteúdo→ppb, busca interna no catálogo, abreviaturas minadas, marca determinística, IVA por voto, categoria fechada, consulta por tokens. Estados no `Analise_Fontes`.

### Busca por nome + Tradução + Resolução por nome (NOVO, 2026-06-15)
- **Busca por nome (autocomplete da lista + nutrição):** índice **`produto_busca`** (migração 059, ~54k, FULLTEXT) materializado do catálogo PT-comprável (lojas PT + Mercadona-ES/Lidl-FR traduzidos, deduplicado) por `scripts/construir_produto_busca.mjs`. **NÃO** o `off_full` (4,5M multilíngue → recall PT baixíssima). `GET /api/produto/autocomplete?q=&modo=lista|nutricao`: BOOLEAN (último token=prefixo, anteriores=palavra exata), ranking palavra-exata>começa-com, **GENÉRICOS no topo** (substantivo-cabeça por frequência + histórico da casa), alimento 1.º na lista; mata os "esquisitos" do prefixo (leite≠leiteira), ~4-17ms. Frontend: dropdown no campo "escrever" da lista, genéricos em **negrito**. Settings globais do FULLTEXT NÃO tocadas (`min_token_size=3`, partilhado).
- **Tradução PT — modelo por IDIOMA da fonte, SOB DEMANDA + persistida** (ver `[[traducao-sob-demanda]]`): `FONTES_PT` (`fichaEan.js`) = só lojas PT fiáveis (Continente/Auchan/PingoDoce/Lidl-PT). Mercadona-ES/Lidl-FR/Leclerc/`mercadona-off` são **estrangeiras** → o nome cai na tradução LLM (`traduz.js`, prompt cobre não-alimentar + descritivos junto a nomes próprios). Traduz só no scan/uso (`consultarProdutoEan(pt=1)` para lista E consulta), **grava em `produto_ean.nome`** e re-leituras reusam (zero LLM). Nunca em lote sobre o OFF (4,5M, inviável). Guarda: `nome_pt`=`nome` (identidade) não conta como tradução. `consolidarProduto` prefere o `produto_ean.nome` persistido ao nome cru do catálogo. Backfill bounded ~14k Mercadona/Lidl→`nome_pt`: `scripts/traduzir_corpus_pt.mjs`.
- **Resolução por NOME de EAN desconhecido** (`normaliza/resolverPorNome.js`, `acharPorNomeMarca`, **LIGADA 2026-06-15** — sugere+confirma): EAN sem nutrição/imagem → busca o **mesmo produto sob OUTRO EAN** no `off_full` (FULLTEXT, marca=gate forte, tamanho desempata). **Ligada na ficha:** `consolidarProduto` (`routes/produto.js`, `/info`) devolve `sugestao_nome` quando a ficha fica magra; a ficha v2 mostra cartão âmbar "achámos o mesmo produto noutro código — usar nutrição/foto?"; ao confirmar, `POST /produto/adotar-nome` (→ `adotarNomeRef`) copia a nutrição (confirmada) + regista `nome_ref_ean` no `fusao` (reversível/auditável); o `/info` resolve a imagem do gémeo pelo off_full. **Decisão do dono: sugerir-e-confirmar, não adotar automático** ("texto acha, humano confirma"). Provado e2e (Pérolas Hacendado). **Falta:** a IMAGEM validar (cruzar `matchImagem.js` CLIP — "imagem confirma") + vetorizar a foto adotada.

### Avisos e backlog vivos
- **Auth Google FECHADO** (ver `docs/Auth_Zitadel.md`); falta rotacionar o PAT (exposto) e SMTP no Zitadel. Allowlist: gviberti3@gmail.com, suerocha@gmail.com.
- **Sudo temporário** `90-bigbag-nopasswd` ainda ativo (instalação não terminou).
- **Continente scrape** bloqueado por anti-bot → cron noturno gota-a-gota (19,1k temos).
- **EANs do talão (Makro) válidos-mas-errados** (VLM troca dígito → outro EAN real); cruzar com a descrição (backlog).
- **Identificação por foto do produto errado:** falta prevenir (`corresponde_nota`) e corrigir (botão refazer no `ProdutoInfoSheet`).
- **Backlog:** eixo "reviews" (qualidade percebida, só processados — decidir se entra na visão); lista preditiva; agregado familiar; Fase C (busca web não-alimentares); `nif_comprador`→membro; famílias equivalentes entre lojas (Fase D3, semente: 3.421 EANs Auchan∩Continente); i18n leftovers; `/identificar` sem transação.

## Decisões em aberto (não inventar — opção segura + assinalar)
1. **Transcrição de voz:** STT separado vs. áudio-direto. **v1:** áudio-direto (`input_audio` base64), trocável em `transcricao.js`. Falta experimentar STT antes de fixar.
2. **OCR no dispositivo** (decisão #6 do Conceito) — só quando for app nativa. *(Leitura de fatura: FECHADA — VLM p/ imagem, texto-PDF+LLM p/ PDF.)*
3. Detalhe e decisões fechadas: `Conceito §10`.
